index.html: 웹 페이지 구동 파일(front)
app.py: 모델 구현 파일(back)

실행 방법(vscode에서)
1. 필요한 라이브러리 설치: pip install flask flask-cors numpy 터미널에 입력
2. app.py 실행: 터미널에 python app.py 입력
3. index.html 실행: vscode에 우측 하단에 LiveServer로 웹에서 열기

#만약 위 방법으로 해서 안되면 app이랑 index port번호 일치하는지 확인하기. 이거 아니면 그때는 GPT ㄱㄱ
